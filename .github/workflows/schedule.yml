name: Update gist
on:
  push:
    branches:
      - master
  schedule:
    - cron: "0 0 * * *"
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - name: Update gist
        uses: maxam2017/productive-box@master
        env:
          GH_TOKEN: ${{ secrets.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 }}
          GIST_ID: ${{ secrets.0ef767793f5c3b3ad178877306431f72 }}
          TIMEZONE: Asia/Seoul
